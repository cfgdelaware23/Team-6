# !pip3 install python-docx

from docx import Document

# We will assume that all of the emails have as stored fields the following: Discription, times, and names
# We will get these from the database
discriptions= []
times= []
names= []

